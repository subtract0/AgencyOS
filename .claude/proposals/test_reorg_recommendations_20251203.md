# Test Reorganization Recommendations

**Date**: 2025-12-03
**Status**: Analysis Complete
**Reviewed By**: Claude Code (autonomous session)

## Executive Summary

After analyzing the test suite structure, I've identified several categories of tests that could benefit from reorganization. The current `tests/legacy/` directory structure is working well for clearly obsolete tests.

## Current Legacy Directory (8 files, working)

```
tests/legacy/
├── test_bash_validation.py
├── test_constitutional_test_agents.py
├── test_generate_deletion_pr.py
├── test_init_ollama_model.py         # Codex fixed: added skip guards
├── test_overnight_integration.py
├── test_property_based.py
├── test_tool_cache.py                 # Codex fixed: removed unused fixture
├── test_v5_integration.py
└── trinity_protocol/                  # Ollama-specific legacy tests
```

## Tests Analysis

### Category 1: TRM-7M Related Tests (KEEP for now)

The TRM-7M feature was **pivoted** (grid puzzles, not code) per `docs/TRM_PIVOT.md`, but the source code still exists:
- `tools/trm_training/` - 3 modules present
- `trinity_protocol/core/trm_validator.py` - 23KB
- **Test file**: `tests/test_trm_validation_layer.py` (15 tests, all collect)

**Recommendation**: Keep for now. The code exists and may be revisited. Move to legacy only if source modules are deleted.

### Category 2: Firestore Tests (KEEP - conditional execution)

Tests with many conditional skips based on `FRESH_USE_FIRESTORE`:
- `tests/test_firestore_learning_persistence.py` (3 skips)
- `tests/test_firestore_mock_integration.py` (0 skips)
- `tests/test_integrations.py` (20 skips)

**Recommendation**: Keep. These are valid integration tests, just conditionally skipped. Already excluded from fast runs via `--ignore` flags.

### Category 3: Ollama-Dependent Tests (ALREADY in legacy/trinity_protocol)

Already properly organized:
- `tests/legacy/trinity_protocol/test_docker_ollama_*.py`
- `tests/legacy/trinity_protocol/core/test_hybrid_executor*.py`

**Recommendation**: Already handled. No action needed.

### Category 4: Potential Candidates for Future Legacy

These tests reference features with uncertain status:
- `tests/test_lean_agent.py` - Tests "lean agent" concept (2 skips)
- `tests/foundation_automation/` - Tests TRM integration (but still active)

**Recommendation**: Monitor. Move if source features are removed.

## Skip Statistics by File

Files with highest skip counts (may need attention):
| File | Skip Count | Notes |
|------|------------|-------|
| tests/test_integrations.py | 20 | Firestore integration - valid skips |
| tests/test_firestore_learning_persistence.py | 3 | Firestore - valid skips |
| tests/test_lean_agent.py | 2 | Monitor |
| tests/test_vectorstore_integration_fix.py | 1 | Active feature |

## Recommended Actions

### Immediate (No Changes Needed)
1. ✅ Current legacy organization is working
2. ✅ `--ignore` flags in `run_tests.py` exclude firestore tests
3. ✅ Codex's skip guards in `test_init_ollama_model.py` are working

### Future (When Source Changes)
1. If `tools/trm_training/` is removed → move `test_trm_validation_layer.py` to legacy
2. If Firestore is fully deprecated → move firestore tests to legacy
3. If lean agent is removed → move `test_lean_agent.py` to legacy

## Metrics

- **Total test files with skips**: 46
- **Files already in legacy**: 8 + trinity_protocol/
- **Candidates for future legacy**: 2-3
- **Tests excluded via --ignore**: 2 (firestore files)

## Conclusion

The current test organization is **satisfactory**. No immediate moves recommended. The legacy directory structure is working well for obsolete tests, and the `--ignore` mechanism handles conditional integration tests.

---

**Generated by**: Agent Evolution autonomous session
**Verified**: Test collection passing for all analyzed files

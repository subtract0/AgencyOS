[project]
name = "night_shift"
version = "0.3.1"
description = "Night Shift tooling"
requires-python = ">=3.10"

# Existing dependencies...
dependencies = [
    "rich>=13.0.0",
]

# Optional dependencies for the TUI (rich is already required)
[project.optional-dependencies]
tui = [
    # No extra deps for the minimal implementation.
]

[tool.setuptools]
packages = ["night_shift"]
